//import global é para ser aproveitdo por toda a aplicação, já que, pode definição cada componente tem um estilo unico dentro do React

//O arquivo index.css já é o css global


//import { createGlobalStyle } from "styled-components";

//export const GlobalStyle = createGlobalStyle`

   
//`